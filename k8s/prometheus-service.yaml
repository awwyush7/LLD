apiVersion: v1
kind: Service
metadata:
  name: prometheus-dashboard
spec:
  selector:
    app: prometheus-dashboard
  ports:
    - port: 9100
      targetPort: 9100
  type: ClusterIP   # view via: kubectl port-forward svc/prometheus-dashboard 9100:9100, then open /dashboard

apiVersion: v1
kind: Service
metadata:
  name: postgres           # other pods reach postgres via "postgres:5432"
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP          # internal only — database should never be public

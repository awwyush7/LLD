apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
spec:
  selector:
    app: ingestion-service
  ports:
    - port: 8003
      targetPort: 8003
  type: LoadBalancer  # minikube tunnel assigns a real IP — use this for load testing instead of port-forward

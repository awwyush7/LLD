apiVersion: v1
kind: Service
metadata:
  name: pgbouncer   # other pods reach the pool via "pgbouncer:6432" instead of "postgres:5432"
spec:
  selector:
    app: pgbouncer
  ports:
    - port: 6432
      targetPort: 6432
  type: ClusterIP
